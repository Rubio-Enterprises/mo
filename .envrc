
# Default: load staging-encrypted env into the dev shell.
# Override locally with .envrc.local (gitignored).
[ -f secrets.staging.enc.yaml ] && use sops secrets.staging.enc.yaml
[ -f .envrc.local ] && source_env .envrc.local

