# Shape doc for encrypted secrets. Committed plaintext, NO real values.
# When you create secrets.staging.enc.yaml or secrets.prod.enc.yaml, mirror
# this shape and run `sops` to edit.

api_key: "<your-google-api-key>"
database_url: "postgres://user:<password>@localhost:5432/dbname"
oauth:
  client_id: "<oauth-client-id>"
  client_secret: "<oauth-client-secret>"
